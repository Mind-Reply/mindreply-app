# RETIRED / PROTECTED
# Legacy SSH production deployment is disabled pending the canonical Cloudflare/ResellerPro release path.
# Historical content is preserved in Git history.

name: Retired SSH Production Deployment Guard

on:
  workflow_dispatch:

jobs:
  guard:
    if: vars.LEGACY_DEPLOY_ENABLED == 'true'
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Legacy SSH production deployment is disabled."
          exit 1
