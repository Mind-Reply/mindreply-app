# RETIRED / PROTECTED
# A11-K no longer deploys from this canonical MindReply repo.
# The former Vercel production workflow is intentionally disabled.
# Canonical deployment must be established in the A11-K repository using the approved Cloudflare/ResellerPro path.

name: Retired A11-K Deployment Guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment boundary
        run: |
          echo "A11-K deployment is intentionally disabled in Mind-Reply/mindreply-app."
          echo "Use the canonical A11-K repository and the verified Cloudflare/ResellerPro release path."
          exit 1
