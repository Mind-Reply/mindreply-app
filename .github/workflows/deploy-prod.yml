# RETIRED / PROTECTED
# Production deploy workflow disabled pending Cloudflare/ResellerPro canonicalisation.
# This workflow is intentionally manual-only and guarded.
# It does not run on repository pushes.

name: Retired Production Deployment Guard

on:
  workflow_dispatch:

env:
  LEGACY_DEPLOY_ENABLED: 'false'

jobs:
  guard:
    if: vars.LEGACY_DEPLOY_ENABLED == 'true'
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Legacy production deployment is disabled. Use the verified Cloudflare/ResellerPro release path."
          exit 1
