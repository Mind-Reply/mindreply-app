# RETIRED / ISOLATED
# This E2E workflow currently fails before job creation in GitHub Actions.
# Functional validation remains covered by Frontier Modern and Code Quality.
# Keep source in Git history; do not advertise E2E coverage as passing.

name: Retired Frontend E2E

on:
  workflow_dispatch:

jobs:
  guard:
    if: vars.LEGACY_E2E_ENABLED == 'true'
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Frontend E2E workflow is isolated pending a clean workflow-start repair."
          exit 1
