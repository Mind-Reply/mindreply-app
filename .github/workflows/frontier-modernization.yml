# RETIRED / DUPLICATE CI PATH
# The canonical validation path is .github/workflows/frontier-modern.yml.
# This workflow is retained only for historical traceability.

name: Retired Frontier Modernization

on:
  workflow_dispatch:

jobs:
  guard:
    if: vars.LEGACY_CI_ENABLED == 'true'
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Duplicate Frontier Modernization workflow is retired. Use Frontier Modern."
          exit 1
