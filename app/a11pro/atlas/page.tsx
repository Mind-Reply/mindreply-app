import Link from "next/link"; import {A11Header,Footer} from "../A11ProSite"; export default function AtlasPage(){const zones=[["CHAT","briefing","Context → objective → authority"],["NEXUS","decision","Decision routes with named ownership"],["FORGE","release","Acceptance → rollback → review"],["STUDIO","signal","Proof-bearing communication"]];return <main className="a11"><div className="site-page"><A11Header/><section className="subhero"><span className="mono">A11pro / Atlas</span><h1>See the whole route<br/>before you move it.</h1><p className="lede">A spatial map of the work surface. Every territory has a purpose, boundary and handoff.</p><div className="rail"><span>OWNER</span><span>COMMAND</span><span>PROOF</span><span>RELEASE</span></div></section><section className="grid">{zones.map(([n,t,p],i)=><article className="card" key={n}><span className="mono">{String(i+1).padStart(2,"0")} / {n}</span><h3>{t}</h3><p>{p}</p><div className="proof">{i===0?"Connection boundary: external action is not implied.":i===1?"Authority boundary: owner approval remains explicit.":i===2?"Release boundary: rollback is required.":"Publishing boundary: evidence reference required."}</div></article>)}</section><p style={{marginTop:30}}><Link className="cta" href="/a11pro/forge">Continue to Forge →</Link></p><Footer/></div></main>}