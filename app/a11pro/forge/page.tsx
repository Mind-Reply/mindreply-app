import Link from "next/link"; import {A11Header,Footer} from "../A11ProSite"; export default function ForgePage(){const gates=["Objective is explicit","Repository is named","Acceptance tests exist","Security constraints are stated","Rollback path exists","Owner approval is still pending"];return <main className="a11"><div className="site-page"><A11Header/><section className="subhero"><span className="mono">A11pro / Forge</span><h1>Build the release<br/>without faking the release.</h1><p className="lede">A deterministic pre-release surface. It prepares work for owner review; it does not claim production deployment.</p></section><section className="grid">{gates.map((g,i)=><article className="card" key={g}><span className="mono">GATE {String(i+1).padStart(2,"0")}</span><h3>{g}</h3><p>{i===5?"Human approval remains intentionally explicit.":"Required evidence boundary before promotion."}</p></article>)}</section><div className="proof">STATUS / READY FOR OWNER REVIEW — not deployed, not settled, not silently executed.</div><p style={{marginTop:30}}><Link className="cta" href="/a11pro/studio">Continue to Studio →</Link></p><Footer/></div></main>}