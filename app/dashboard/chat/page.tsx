"use client";
import { DashboardShell, PagePanel } from "../DashboardShell";
import { useState } from "react";
export default function ChatPage(){const [q,setQ]=useState("");const [draft,setDraft]=useState("");return <DashboardShell title="Private Chat"><PagePanel heading="Command the desk" description="Ask for a recommendation. Suggestions become drafts and stop at the approval queue."><div className="card"><textarea value={q} onChange={e=>setQ(e.target.value)} placeholder="What should I move next?" style={{width:"100%",minHeight:120,background:"transparent",color:"inherit",border:0,outline:0,fontSize:16}}/><div className="actions"><button onClick={()=>setDraft(q?`Suggested move: ${q}. Review the warmest client thread and prepare a concise next step.` : "Suggested move: review the warmest open client thread.")}>Draft</button><button className="quiet" onClick={()=>setQ("")}>Clear</button></div>{draft&&<div className="queue" style={{marginTop:16}}><div><b>Suggested action</b><span>{draft}</span></div><div className="actions"><button>Queue</button><button className="quiet">Edit</button><button className="quiet">Block</button></div></div>}</div></PagePanel></DashboardShell>}