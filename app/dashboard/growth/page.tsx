import { DashboardShell, PagePanel } from "../DashboardShell";
export default function GrowthPage(){return <DashboardShell title="Growth"><PagePanel heading="Growth board" description="Twenty useful moves are generated as proposals. Nothing is sent or posted without approval."><div className="move-grid">{Array.from({length:20},(_,i)=><article className="card move" key={i}><span className="eyebrow">MOVE {String(i+1).padStart(2,"0")}</span><p>{["Recover a warm lead","Clarify an offer","Ask for the next step","Capture proof","Improve one route","Draft a client answer"][i%6]}</p><div className="actions"><button>Draft</button><button className="quiet">Queue</button><button className="quiet">Block</button></div></article>)}</div></PagePanel></DashboardShell>}