import { DashboardShell, PagePanel } from "../DashboardShell";
export default function SettingsPage(){return <DashboardShell title="Settings"><PagePanel heading="Connections & safeguards" description="Configuration is intentionally visible as state, never as credential values."><div className="move-grid">{[["Cloudflare","Worker API + D1-ready storage","Not connected"],["n8n","Automation webhooks","Not connected"],["Salesforce","Lead + Task flow","Not connected"],["GitHub","Source + release evidence","Connected"],["External actions","Messages / posts / payments","Blocked by default"]].map(x=><article className="card move" key={x[0]}><span className="eyebrow">{x[0]}</span><p>{x[1]}</p><strong>{x[2]}</strong><div className="actions"><button>Open</button><button className="quiet">Check</button></div></article>)}</div></PagePanel></DashboardShell>}