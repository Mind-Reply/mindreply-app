import { DashboardShell, PagePanel, MoveCard } from "../DashboardShell";
export default function TodayPage(){return <DashboardShell title="Today"><PagePanel heading="Six decisions, not six dashboards" description="A compact operating brief for the next useful moves."><div className="move-grid">{[["Money move","Follow the highest-intent opportunity","Open"],["Client move","Resolve the oldest unanswered question","Draft"],["Proof move","Verify the latest live route","Check"],["Risk move","Review blocked outbound work","Open"],["Visibility move","Prepare one useful insight","Draft"],["Owner move","Clear the approval queue","Approve"]].map(x=><MoveCard key={x[0]} kind={x[0]} text={x[1]} action={x[2]}/>)}</div></PagePanel></DashboardShell>}