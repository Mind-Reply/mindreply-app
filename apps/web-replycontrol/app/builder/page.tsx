import { BuilderShell } from "./BuilderShell";

export const metadata = {
  title: "ResellerPro Builder — MindReply",
  description: "Prompt, edit, configure, preview and publish a production-ready website or app.",
};

export default function BuilderPage() {
  return <BuilderShell />;
}
